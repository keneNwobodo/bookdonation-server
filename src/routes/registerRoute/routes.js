const { Router } = require("express")

const router = Router()

router.get("/register", {})
router.post("/register", {})
router.get("/login", {})
router.post("/login", {})
router.get("/logout", {})

module.exports = router